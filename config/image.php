<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Default Image Driver
    |--------------------------------------------------------------------------
    |
    | This option controls the default image "driver" used by Laravel. This
    | driver is used when another is not explicitly specified when executing
    | a command such as `ResizeImage`. Supported: "gd", "imagick"
    |
    */

    'driver' => 'imagick',
];
